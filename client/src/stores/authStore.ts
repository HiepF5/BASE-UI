// ============================================================
// Auth Store - Re-export from auth module (backward compatibility)
// ============================================================

export { useAuthStore } from '../modules/auth/store/auth.store';
export type { AuthUser, AuthState } from '../modules/auth/types/auth.types';
